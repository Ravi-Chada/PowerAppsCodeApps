/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 */
import type { PluginDefinition } from '../PluginDefinition.js';
export declare const GetContextKey: "AppLifecycle.getContext";
export interface IContext {
    app: IAppContext;
    host: IHostContext;
    user: IUserContext;
}
export interface IAppContext {
    appId: string;
    appSettings: object;
    environmentId: string;
    queryParams: Record<string, string>;
    /**
     * URL of the Dataverse organization linked to the current environment, when
     * the environment has a linked Dataverse instance. Optional — apps that
     * don't use Dataverse, environments without a linked Dataverse instance,
     * and hosts that don't yet populate this field will leave it `undefined`.
     */
    dataverseOrgUrl?: string;
}
export interface IHostContext {
    sessionId: string;
}
export interface IUserContext {
    fullName?: string;
    objectId?: string;
    tenantId?: string;
    userPrincipalName?: string;
}
export type GetContextHandler = () => Promise<IContext>;
export type GetContextPluginDefinition = PluginDefinition<typeof GetContextKey, GetContextHandler>;
//# sourceMappingURL=GetContextPlugin.d.ts.map