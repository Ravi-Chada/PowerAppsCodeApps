/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 */
declare global {
    var powerAppsBridge: PowerAppsBridge | undefined;
}
/** @private */
export type PowerAppsBridge = {
    initialize(): Promise<void>;
    executePluginAsync<T = unknown>(pluginName: string, pluginAction: string, params: unknown[], onUpdate?: (args: unknown) => void): Promise<T>;
    /**
     * Optional hook for runtimes that need custom request body serialization.
     * When present, RuntimeDataClient will delegate body construction to this
     * method instead of its default Blob-wrapping logic.
     * Native bridges (e.g. Hermes / React Native) implement this to avoid
     * `new Blob([ArrayBuffer])` which is unsupported by Hermes.
     */
    createRequestBody?(config: {
        body?: string | Uint8Array | ArrayBuffer | Blob;
        headers?: {
            [key: string]: string;
        };
    }): Blob | string | Uint8Array | ArrayBuffer;
};
/** @private */
export declare function executePluginAsync<T = unknown>(pluginName: string, pluginAction: string, params?: unknown[], update?: (args: unknown) => void): Promise<T>;
/** @private */
export declare function getBridge(): Promise<PowerAppsBridge>;
//# sourceMappingURL=PluginBridge.d.ts.map